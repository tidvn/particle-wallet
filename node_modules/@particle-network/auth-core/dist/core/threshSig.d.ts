import type { Ecdsa, EdDsa } from '@particle-network/thresh-sig';
declare const threshSig: {
    edDSA(data: string): Promise<EdDsa>;
    ecDSA(data: string): Promise<Ecdsa>;
    refreshEcdsa(p2Key: string): Promise<string>;
    decryptData(ciphertext: string, key: string): Promise<string>;
    encryptData(plaintext: string, key: string): Promise<string>;
    generateKey(password: string, uuid: string): Promise<string>;
    create(chainName: string): Promise<{
        p2Key: string;
        address: string;
    }>;
    params(): string;
};
export default threshSig;
