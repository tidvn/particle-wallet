import type { AuthOptions } from './../core/authCore';
type Env = {
    basicUsername: string;
    basicPassword: string;
    baseUrl: string;
    rpcUrl: string;
    authCore: string;
};
declare class GlobalConfig {
    #private;
    initialized: boolean;
    paymentToken: string | undefined;
    init(authOptions: AuthOptions): void;
    get authOptions(): AuthOptions;
    get version(): string;
    get deviceId(): string;
    get env(): Env;
}
declare const globalConfig: GlobalConfig;
export default globalConfig;
