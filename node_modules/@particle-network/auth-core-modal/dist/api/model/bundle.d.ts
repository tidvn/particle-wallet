export interface AuthorizationOptions {
    message?: string;
    uniq?: boolean;
}
export declare enum PromptSettingType {
    /**
     * 不提醒
     */
    none = 0,
    /**
     * 首次提醒
     */
    first = 1,
    /**
     * 每次都提醒
     */
    every = 2,
    /**
     * 每次都提醒，且不可跳过
     */
    everyAndNotSkip = 3
}
export interface PromptSettingConfig {
    promptPaymentPasswordSettingWhenSign?: PromptSettingType | boolean;
    promptMasterPasswordSettingWhenLogin?: PromptSettingType | boolean;
}
