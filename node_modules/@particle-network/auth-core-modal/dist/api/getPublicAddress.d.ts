import { type ERC4337Options } from '@particle-network/auth-core';
type Options = {
    chainId?: number;
    erc4337?: ERC4337Options;
};
export declare const getEVMPublicAddress: ({ chainId, erc4337 }: Options) => Promise<string>;
export {};
