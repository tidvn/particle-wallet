import type { ConnectParam } from '@particle-network/auth-core';
import { type Chain } from '@particle-network/chains';
import type { Authorization, ConnectWithSocialOptions } from '../../types';
declare const useLogin: () => {
    loginHandler: (data: ConnectParam, emitError?: boolean) => Promise<void>;
    socialAuthLogin: (options: ConnectWithSocialOptions & {
        authorization?: Authorization;
        chain?: Chain;
    }) => Promise<void>;
};
export default useLogin;
