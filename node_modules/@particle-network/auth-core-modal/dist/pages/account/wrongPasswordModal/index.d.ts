import React from 'react';
export interface WrongPasswordInfo {
    visible: boolean;
    accountFrozen?: {
        seconds: number;
    };
}
declare const WrongPasswordModal: ({ info }: {
    info: WrongPasswordInfo;
}) => React.JSX.Element;
export default WrongPasswordModal;
