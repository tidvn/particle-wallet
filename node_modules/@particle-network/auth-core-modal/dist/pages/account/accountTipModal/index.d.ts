import React from 'react';
export interface AccountTipModalProps {
    visible: boolean;
    confirm?: () => void;
}
declare const AccountTipModal: ({ modal }: {
    modal: AccountTipModalProps;
}) => React.JSX.Element;
export default AccountTipModal;
