import type { Chain, EVMProvider, Wallet } from '../../types';
import { EVMConnector } from './base';
import type { WalletConnectOptions } from './wallet-connect-v2';
export declare class MetaMaskConnector extends EVMConnector {
    #private;
    private metamaskProvider?;
    constructor({ chains, getProvider }: {
        chains?: Chain[];
        getProvider: () => any;
    });
    connect(): Promise<EVMProvider>;
    disconnect(): Promise<void>;
    getProvider(): Promise<any>;
    private onAccountsChanged;
    private onChainChanged;
    private onDisconnect;
}
export declare const metaMask: (options: Omit<WalletConnectOptions, 'chains'>) => Wallet;
