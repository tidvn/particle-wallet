import type { SolanaProvider, DownloadUrls, Wallet } from '../../types';
import { SolanaConnector } from './base';
export interface SolanaWalletConfig {
    package: any;
    options?: any;
    display: {
        name: string;
        iconUrl: string;
        downloadUrls?: DownloadUrls;
    };
}
export default class SolanaWalletAdapterConnector extends SolanaConnector {
    private opts;
    constructor(opts: SolanaWalletConfig);
    connect(): Promise<SolanaProvider>;
    disconnect(): Promise<void>;
}
export declare const solanaWalletAdapter: (config: SolanaWalletConfig) => Wallet;
